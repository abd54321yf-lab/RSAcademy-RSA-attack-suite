# RSAcademy – التقرير الأكاديمي الكامل

## الملخص التنفيذي

RSAcademy هو مشروع تعليمي في الأمن السيبراني والتحليل الجنائي الرقمي يطبق استرجاعًا قائمًا على التواقيع للملفات من البيانات الثنائية وصور الأقراص. يعتمد الإصدار الحالي على Pure Python، ويجمع بين Magic Bytes، ومطابقة Header/Footer، والتحقق البنيوي الأساسي، وحساب Shannon Entropy، وبناء نماذج للأثار المستخرجة، وحساب Hashes، وإنتاج تقارير JSON وHTML وASCII.

المشروع جاهز كنموذج أكاديمي عملي قابل للتشغيل والشرح. وقد تم التحقق منه عبر أربع اختبارات ناجحة، كما تم تشغيله على عينة ثنائية أنتجت أثر JPEG وأثر PDF وتقارير فعلية.

## المشكلة والدافع

قد تحتوي صورة القرص أو الملف التالف على أجزاء من ملفات لم تعد مرتبطة بأسماء أو مجلدات نظام الملفات. لذلك لا يكفي الاعتماد على امتداد الملف أو جدول الملفات. يستخدم المشروع أنماطًا معروفة في بداية الملف ونهايته لتحديد نطاق مرشح، ثم يطبق فحوصًا إضافية قبل تسجيل النتيجة.

## الأهداف

يهدف النظام إلى بناء مسار واضح وقابل للتوسع يبدأ من مصدر ثنائي وينتهي بملفات مستخرجة وتقرير جنائي. كما يهدف إلى فصل منطق الاستخراج عن التحليل والتقارير وواجهة المستخدم، مع توفير Backend اختياري يمكن توسعته لاحقًا باستخدام YARA دون جعل المشروع مجرد غلاف لمكتبة خارجية.

## المعمارية

يتكون التدفق من Input، ثم BinaryReader، ثم اختيار Backend، ثم Header/Footer Matcher، ثم Validator، ثم RsaArtifact Model، ثم التحليل الجنائي، وأخيرًا التقارير والتصدير. يعتمد Batch على استدعاء مسار الملف الواحد، مما يمنع تكرار منطق الاستخراج.

| المكون | المسؤولية |
|---|---|
| `rsa/signature.py` | تعريف FileSignature وقاعدة التواقيع |
| `rsa/reader.py` | التحقق من المسار والقراءة المحدودة |
| `rsa/matcher.py` | البحث عن Headers وFooters |
| `rsa/entropy.py` | حساب وتصنيف Shannon Entropy |
| `rsa/validator.py` | فحوص بنيوية أساسية |
| `rsa/fragment.py` | نموذج الأثر وحل التداخل |
| `rsa/pipeline.py` | تنسيق تحليل ملف واحد أو عدة ملفات |
| `backends/` | فصل Backend Python وYARA الاختياري |
| `analysis/` و`map/` | Metadata، التحذيرات، وOffset Map |
| `output/` | JSON وHTML وASCII |
| `cli/` | الأوامر وواجهة الاستخدام |

## التنفيذ والخوارزميات

تبدأ المطابقة بالبحث عن Header لكل توقيع. عند العثور عليه، يبحث النظام عن Footer ضمن حد الحجم الأقصى للتوقيع. إذا وجد النهاية، يسجل النطاق. وإذا لم يجدها، يسجل الأثر كمبتور بدل أن يخفي المشكلة. بعد ذلك يتحقق Validator من التوقيع وبعض شروط الصيغة، ثم يحسب النظام MD5 وSHA-256 وEntropy.

تستخدم دالة Entropy توزيع تكرار كل قيمة Byte، ثم تطبق Shannon Entropy. النتيجة تقع بين صفر وثمانية Bits per Byte. القيمة المرتفعة قد تتوافق مع ضغط أو تشفير أو بيانات packed، لكنها لا تكفي وحدها لإثبات وجود برمجية خبيثة.

## حالة المراحل الخمس عشرة

المراحل من 1 إلى 4 مكتملة في النواة. المرحلة الخامسة مكتملة جزئيًا بسبب اعتماد Validators الحالية على فحوص مبسطة. المراحل من 6 إلى 8 موجودة في نموذج الأثر وحل التداخل ودمج المحرك. المرحلة التاسعة توفر Python Backend فعليًا وحدًا اختياريًا لـ YARA يحتاج إلى Ruleset. المراحل من 10 إلى 12 توفر خرائط Offsets وHashes وتحذيرات Entropy دون تحليل كامل لنظام الملفات. المرحلتان 13 و14 مكتملتان في التقارير والتصدير وCLI وBatch. المرحلة 15 مكتملة في الاختبارات والتوثيق الأساسي.

## الاختبارات والنتائج

تم تنفيذ أربعة اختبارات بنجاح. يغطي الاختبار الأول حدود Entropy، ويغطي الثاني مطابقة JPEG، ويغطي الثالث تشغيل Pipeline وتوليد التقارير، ويغطي الرابع استمرار Batch بعد إدخال ملف مفقود. كما تم تشغيل `compileall` على المشروع وتجربة Batch على عينتين.

المخرجات الفعلية تشمل `report.json` للمعالجة الآلية، و`report.html` للعرض الجنائي، و`disk_map.txt` للعرض النصي، ومجلد `extracted_files` للملفات الناتجة.

## الأهمية الجنائية والأمنية

يساعد Offset في تحديد موضع الأثر داخل المصدر وإعادة فحصه. وتساعد Magic Bytes في اكتشاف ملفات لا تحمل أسماء أو امتدادات مفيدة. ويسجل النظام التحذيرات عندما يكون Footer مفقودًا أو عندما يكون Entropy مرتفعًا. أما Slack Space والتصنيف المتقدم للبيانات المخفية فيحتاجان إلى معرفة بنظام الملفات حتى لا يتحول الاستنتاج إلى تخمين.

## القيود

لا يدعي الإصدار الحالي استرجاع الملفات شديدة التجزئة بدقة كاملة. كما لا يحتوي على Parser كامل لأنظمة الملفات، ولا يضمن استعادة الحاويات المضغوطة المعقدة دون سياق. ويحتاج تحليل الصور الضخمة جدًا إلى Streaming matcher مع Carry-over بين الكتل في إصدار لاحق. كما أن YARA واجهة اختيارية وليست Ruleset جاهزًا.

## التطوير المستقبلي

تتضمن الأولويات التالية إضافة Validators أعمق لـ JPEG وPDF وZIP، وإضافة تحليل Filesystem-aware للـ Slack Space، وإنشاء SVG وخرائط HTML أكثر تفاعلية، وإضافة Loader لقواعد YARA، وإضافة TOML بعد التحقق من الإعدادات، ثم دراسة Parallel Processing بعد تثبيت اختبارات الأداء والصحة.

## التشغيل

```bash
python3 main.py --help
python3 main.py carve samples/mixed.bin --output results
python3 main.py batch samples --recursive --pattern '*.bin' --output batch-results
python3 -m unittest discover -s tests -v
```

## الخلاصة

يقدم RSAcademy أساسًا واضحًا وقابلًا للدفاع الأكاديمي في Digital Forensics. قوته الأساسية هي الفصل المعماري، والشفافية في تسجيل النتائج، ووجود اختبارات وتوثيق، والاعتراف الصريح بالقيود. أما التحليل الجنائي المتقدم فيمثل مسار توسعة لاحقًا وليس ادعاءً مضمنًا في النسخة الحالية.

## References

[1]: https://docs.python.org/3/library/hashlib.html "Python hashlib documentation"

[2]: https://docs.python.org/3/library/math.html "Python math documentation"

[3]: https://docs.python.org/3/library/argparse.html "Python argparse documentation"
